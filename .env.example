# Frontend origin allowed by CORS (must include scheme and port). Example for local dev:
# FRONTEND_ORIGIN=http://localhost:3000
FRONTEND_ORIGIN=

# Use secure cookies only over HTTPS in production (true/false)
# In development (HTTP), set to false. In production (HTTPS), set to true.
COOKIE_SECURE=false

# Session TTL in seconds (default 86400 = 24h)
SESSION_TTL_SECONDS=86400

# GitHub OAuth credentials
GH_CLIENT_ID=
GH_CLIENT_SECRET=

# Database (MySQL) connection
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_NAME=

# Optional Redis URL for session persistence in production
# Example: redis://localhost:6379/0 or rediss://<host>:<port>/<db>
REDIS_URL=
